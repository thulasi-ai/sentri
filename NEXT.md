# NEXT.md — Current Sprint Target

> **For agents:** Read this file only. Do not read ROADMAP.md unless you need context on items
> beyond the current PR. Everything you need to start work is here.
>
> **For humans:** Update this file when a PR ships. Move the completed item to ROADMAP.md ✅ table,
> promote the next item from the queue below, and rewrite the "Current PR" block.

> **Bundling guidance — for agents writing code:** When working on the Current PR, if you
> spot adjacent items in the Queue (or in `ROADMAP.md`) that share files, infrastructure,
> or a natural review boundary with the in-flight scope, **flag them as bundling candidates
> in your PR description** rather than expanding the PR mid-flight. Good bundling signals:
> (1) the items touch the same module / shared abstraction, so reviewing them together
> reduces churn (e.g. CAP-004 + MET-001 share `<TrendChart>`); (2) one item validates
> another end-to-end (e.g. a CI guard validates the convention it documents);
> (3) both are S/XS effort and skipping a hand-off cycle saves more than it costs in
> review surface (e.g. AUTO-017.3 + PROC-001 in slot 2). **Bad** bundling signals: items
> in different phases, items that grow the PR past M effort, items that change the
> reviewer's mental model (UX rewrite + backend rewrite), or items the agent identifies
> *after* CI is already green on the original scope. When in doubt, surface the candidate
> bundle as a comment on the PR and let the human decide — never silently expand scope
> beyond the Current PR's `### PR checklist`. Recording the rejected candidates is also
> useful: it builds the dataset for future planning.

---

## ▶ Current PR — INT-002b — GitHub integration polish (installation UX + App-level webhooks)
**Effort:** M | **Priority:** 🔵 Medium | **Dependencies:** INT-002 ✅ (PR #15 — reuses `verifyWebhookSignature("github", …)`, `getInstallationToken()` cache, `githubCheckSettingsRepo`) | **Source:** `ROADMAP.md` Phase 3 (INT-002b — filed during PR #15 review per AGENT.md issue-handling rule)
> INT-002 ✅ shipped in PR #15 (GitHub App-based Check Run client with TTL-cached installation tokens, `queued → in_progress → success/failure/neutral` lifecycle, HMAC-verified `/trigger/github` endpoint, X-GitHub-Delivery idempotency via cross-dialect LIKE lookup, regressed-tests-only summary with explicit fallback, separate Web Vitals violations bullet, per-project Settings → Integrations tab). Two `TODO(INT-002b):` markers were left in the code at the exact gaps — `frontend/src/pages/Settings.jsx:~1114` (generic `https://github.com/apps` deep-link instead of OAuth-style install callback) and `backend/src/routes/trigger.js:~759` (no App-level webhook receiver — `installation.deleted` events have no project token and aren't routed). Promoting INT-002b ahead of AUTO-004 because the polish gaps are observable today (operators have to hand-paste numeric installation IDs; uninstalling the App leaves stale `enabled=1` rows that silently 401 on every subsequent PR delivery), and the implementation reuses every helper INT-002 already shipped — so this is a small, focused polish PR rather than expanding scope mid-flight.
INT-002 shipped the core PR check-run lifecycle but left two operator-facing gaps documented as `TODO(INT-002b):` markers in the code: (1) installation UX is hand-rolled — admins paste numeric `installationId` + `owner/repo` into the Settings form because the "Install GitHub App" button is a generic deep-link to `https://github.com/apps` rather than an OAuth-style callback; (2) when an admin uninstalls the App on GitHub, the per-project rows in `github_check_settings` keep `enabled=1` + stale `installationId` and subsequent PR deliveries silently 401 against `/app/installations/.../access_tokens` (swallowed by `concludeGithubCheck`'s log-and-swallow contract — observable in logs only). A third gap (encrypting `installationId`) was filed as **WONTFIX** in ROADMAP.md § INT-002b — it's a public numeric identifier (visible in GitHub's UI, request URLs, and webhook payloads), so AES-encrypting it would add key-management cost with no security benefit. This PR closes the two real gaps.
**Implementation sketch:**
- New `backend/src/routes/integrations/github.js`: callback handler `GET /api/v1/integrations/github/install/callback?installation_id=<n>&setup_action=<install|update>&state=<jwt>` (admin-gated) — verifies the `state` JWT (signed with `JWT_SECRET`, contains `projectId` + 10-minute expiry), calls the new `getInstallationRepos(installationId)` to fetch the user-selected repo set, then upserts `github_check_settings` with `enabled=1` + `installationId` + `repo`. Plus App-level webhook handler `POST /api/v1/integrations/github/app-webhook` — HMAC-verified via the existing `verifyWebhookSignature("github", req.rawBody, sig)`, NOT `requireTrigger` (these events are App-wide, no project token).
- `backend/src/integrations/githubChecks.js`: extend with three helpers — `getInstallationRepos(installationId)` (paginated `GET /installation/repositories`, reuses TTL-cached token + bounded retry), `signInstallState(projectId)` (10-minute JWT), `verifyInstallState(token)` (returns `{ projectId }` or null on tamper / expiry).
- `backend/src/database/repositories/githubCheckSettingsRepo.js`: add `disableByInstallationId(installationId)` (SQL `UPDATE … SET enabled=0` + return affected `projectIds[]`), `disableByRepo(installationId, repo)` (narrows to one row), `getByInstallationId(installationId)` (used by the App-webhook handler to enumerate affected projects for activity logging).
- `backend/src/middleware/appSetup.js`: extend `_RAW_BODY_PATH_PATTERN` to cover `/integrations/github/app-webhook` (currently scoped to `/projects/[^/]+/trigger/(?:vercel|netlify|github)/?$`).
- `backend/src/middleware/permissions.json`: register `GET /integrations/github/install/callback` (`role: admin`), `POST /integrations/github/app-webhook` (HMAC-only, public — document explicitly).
- `frontend/src/pages/Settings.jsx`: replace the generic `https://github.com/apps` deep-link (currently flagged with `TODO(INT-002b):`) with a per-row "Install GitHub App" button that calls `api.getGithubInstallStartUrl(projectId)`, then redirects to GitHub's App-install URL with `state` + `setup_url` query params. On callback return, refresh the settings list and show a success toast.
- `frontend/src/api.js`: add `getGithubInstallStartUrl(projectId)` (returns `{ url }` for the redirect).
- App-webhook handler dispatch: `installation.deleted` → `disableByInstallationId(installationId)` + log `integration.github.disabled` activity per affected project (workspace-scoped). `installation_repositories.removed` → `disableByRepo(installationId, repository.full_name)` per row in `repositories_removed[]`. `installation.created` / `installation.suspend` / `installation.unsuspend` → log `integration.github.<event>` and no-op (admins re-enable per project via Settings UI).
- New `backend/tests/github-install-callback.test.js` (registered in `run-tests.js`): state JWT validation (happy path, tamper, expiry), callback upserts `github_check_settings`, App-webhook `installation.deleted` disables only matching rows, `installation_repositories.removed` narrows correctly, missing/invalid HMAC signature returns 401.
- Cleanup: remove the two `TODO(INT-002b):` markers in `frontend/src/pages/Settings.jsx` and `backend/src/routes/trigger.js` once the corresponding code lands.

  **Files:** new `backend/src/routes/integrations/github.js` · `backend/src/integrations/githubChecks.js` (add `getInstallationRepos`, `signInstallState`, `verifyInstallState`) · `backend/src/database/repositories/githubCheckSettingsRepo.js` (add `disableByInstallationId`, `disableByRepo`, `getByInstallationId`) · `backend/src/middleware/appSetup.js` (extend `_RAW_BODY_PATH_PATTERN`) · `backend/src/middleware/permissions.json` (register the two new endpoints) · `backend/src/index.js` (mount the new router under `/api/v1/integrations/github`) · `frontend/src/pages/Settings.jsx` (replace the `TODO(INT-002b):` install-link block with the OAuth-style flow) · `frontend/src/api.js` (add `getGithubInstallStartUrl`) · `backend/src/routes/trigger.js` (remove the `TODO(INT-002b):` marker once the App-webhook receiver lands) · new `backend/tests/github-install-callback.test.js` (registered in `run-tests.js`) · `docs/api/projects.md` (document the App-webhook + callback surface) · `docs/changelog.md` (`### Added` entries)

  **Acceptance criteria:**
- An admin clicks "Install GitHub App" on a project's Integrations row, completes the GitHub install flow (picking org + repo set), lands back on Sentri with `installationId` + `repo` auto-populated and `enabled=1`. Zero manual paste of numeric IDs.
- Uninstalling the App on GitHub (admin → org Settings → Installed GitHub Apps → Uninstall) flips every project row matching that `installationId` to `enabled=0` within ~2s of the webhook firing. An `integration.github.disabled` activity row is emitted per affected project (workspace-scoped). Subsequent PR deliveries to those projects ack `200 { ignored: true }` (no stale 401-against-GitHub spam in logs).
- Removing a specific repo from the App installation (without uninstalling) narrows the disable to just that repo via `disableByRepo` — other projects on the same installation keep working.
- HMAC verification on `/integrations/github/app-webhook` reuses the existing `verifyWebhookSignature("github", …)` path — same `GITHUB_WEBHOOK_SECRET`, same SHA-256 algo, same `X-Hub-Signature-256` header.
- `installation.created`, `installation.suspend`, `installation.unsuspend` events are received and logged but no-op on `github_check_settings` (admin re-enables per project explicitly via Settings UI to avoid silently re-flipping `enabled=1` on an unsuspend they didn't initiate from Sentri).
- `state` JWT replay protection: same `state` token cannot be reused for a second callback (10-minute expiry + one-shot via signed nonce stored in Redis with `EX 600`).
- Both `TODO(INT-002b):` markers in `frontend/src/pages/Settings.jsx` and `backend/src/routes/trigger.js` are removed in this PR (per AGENT.md issue-handling: every TODO either gets resolved or stays with an updated reference).
  **Anti-patterns to reject in review:** wiring the App-webhook through `requireTrigger` (App-level events have no project token — would require manufacturing a synthetic project token, which inverts the security model); silently re-enabling projects on `installation.unsuspend` (suspend ≠ uninstall, but the admin chose to disable; re-enabling without an explicit UI action would surprise them); encrypting `installationId` (filed as WONTFIX in ROADMAP.md § INT-002b — public ID, no security benefit); coupling the install callback to user-OAuth (the GitHub *App* install flow is distinct from OAuth user-auth — don't introduce a second OAuth state machine); skipping `state` JWT replay protection (an attacker who captures the redirect URL could re-bind their own GitHub installation to a different project — the one-shot Redis nonce is the cheapest fix).
### PR checklist (INT-002b)
- [ ] New `backend/src/routes/integrations/github.js` — admin-gated `GET /install/callback` + HMAC-only `POST /app-webhook`; mounted in `backend/src/index.js` under `/api/v1/integrations/github`
- [ ] `backend/src/integrations/githubChecks.js` extended with `getInstallationRepos`, `signInstallState`, `verifyInstallState` reusing the existing TTL-cached installation-token + bounded-retry path
- [ ] `backend/src/database/repositories/githubCheckSettingsRepo.js` extended with `disableByInstallationId(installationId)`, `disableByRepo(installationId, repo)`, `getByInstallationId(installationId)`
- [ ] `backend/src/middleware/appSetup.js` `_RAW_BODY_PATH_PATTERN` extended to cover `/integrations/github/app-webhook` (the App webhook needs raw body for HMAC, same as `/trigger/github`)
- [ ] `backend/src/middleware/permissions.json` registers `GET /integrations/github/install/callback` (admin) and `POST /integrations/github/app-webhook` (HMAC-only / public — explicitly documented)
- [ ] `frontend/src/pages/Settings.jsx` replaces the `TODO(INT-002b):` install-link block with the OAuth-style flow; `frontend/src/api.js` gains `getGithubInstallStartUrl(projectId)`
- [ ] `installation.deleted` disables every project row matching `installationId` and emits `integration.github.disabled` activity per affected project; `installation_repositories.removed` narrows to `(installationId, repo)`; `installation.created` / `suspend` / `unsuspend` log + no-op
- [ ] `state` JWT 10-minute expiry + one-shot Redis nonce (`EX 600`) prevents replay
- [ ] Both `TODO(INT-002b):` markers in `frontend/src/pages/Settings.jsx` and `backend/src/routes/trigger.js` removed
- [ ] `backend/tests/github-install-callback.test.js` registered in `run-tests.js`: state JWT happy-path / tamper / expiry / replay, callback upserts `github_check_settings`, App-webhook `installation.deleted` disables only matching rows, `installation_repositories.removed` narrows correctly, missing/invalid HMAC returns 401
- [ ] `docs/api/projects.md` documents the App-webhook + callback surface; `docs/changelog.md` updated under `## [Unreleased]`
- [ ] No orphan backend routes (PROC-001) — both new endpoints have UI consumers (`Settings.jsx`) or are explicitly machine-only with the `[no-ui]` opt-out documented for the App-webhook

---

## ⏭ Queue (next 3 PRs after current)
### 1 · AUTO-004 — Test impact analysis from git diff
**Effort:** M | **Priority:** 🟢 Differentiator | **Dependencies:** AUTO-002 ✅ (PR #12), AUTO-001 ✅ (PR #15 — risk scorer already consumes `changedPages[]`; AUTO-004 expands the same scoring path with file-level affinity), INT-002 ✅ (PR #15 — GitHub PR-files API path already wired) | **Source:** `ROADMAP.md` Phase 4 (AUTO-004)
Today Sentri runs every approved test on every CI trigger. With AUTO-002's baseline mechanism in place and AUTO-001's risk scorer consuming `changedPages[]`, the next step is mapping file-level git-diffs to affected tests: when a PR touches `src/checkout/CartPage.tsx`, only the tests whose crawl snapshots include elements from that component should run. The mapping is built by cross-referencing each test's `sourceUrl` + captured elements against the file paths extracted from the git diff (via GitHub's PR files API, already available on the INT-002 webhook path), then unioning the file→URL mapping with AUTO-002's `changedPages[]` signal and feeding the union into AUTO-001's scorer as an additional file-affinity boost term.
**Files:** new `backend/src/pipeline/impactAnalysis.js` · `backend/src/integrations/githubChecks.js` (add `getChangedFilesForPr`) · `backend/src/routes/trigger.js` (plumb `changedFiles` into the risk path) · `backend/src/pipeline/riskScorer.js` (file-affinity boost) · `backend/src/database/repositories/runRepo.js` (add `changedFiles` to `JSON_FIELDS` + `INSERT_COLS`) · new migration `022_run_changed_files.sql` · `frontend/src/pages/RunDetail.jsx` (Impact scope panel) · new `backend/tests/impact-analysis.test.js`

**Acceptance criteria:**
- `changedFiles: ["src/checkout/CartPage.tsx"]` (auto-fetched from GitHub's PR files API on an INT-002 delivery, or body override) scopes the run to tests whose snapshots touched `/checkout/*` URLs.
- Empty `changedFiles` (or absent) falls back to current behaviour — zero regression for the JWT path and non-GitHub trigger-token path.
- Unknown file paths (schema migrations, docs, config) produce an empty subset → run marked `skipped_no_impact` rather than running the full suite.
- Merges with AUTO-002's `changedPages[]` — file-affected AND DOM-changed gets the strongest combined signal at the top of the risk-ordered queue.
- GitHub PR-files fetch failure falls back to the full suite — never blocks the run, mirroring INT-002's swallow-and-log contract.
### 2 · CAP-001 — Data-driven testing (parameterized iterations)
**Effort:** M | **Priority:** 🟢 Differentiator | **Dependencies:** none | **Source:** `ROADMAP.md` Phase 4 (CAP-001)
Generated tests are single-shot — one assertion path, one input set. Industry-standard practice (Cypress, Playwright `test.describe.serial` + fixtures, Mabl iterations) is to run the same test against N data rows from a CSV / JSON fixture, with one Run row per iteration so failures are attributable to a specific row. Sentri has no fixture concept today, so testing edge-case data combinations means hand-authoring N near-identical tests. Add per-test fixture upload (CSV / JSON) stored as a `test_fixtures` table row; extend the runner to iterate over fixture rows when present, substituting placeholders in `playwrightCode` (e.g. `{{email}}` → row value).
**Files:** new migration — `test_fixtures` table keyed on `(testId, version)` with `format` (`"csv"` | `"json"`), `rows` (TEXT JSON), `createdAt` · new `backend/src/database/repositories/testFixtureRepo.js` · `backend/src/runner/executeTest.js` (iterate over fixture rows, emit per-iteration result rows with `iterationIndex`) · `backend/src/routes/tests.js` (`POST /api/v1/tests/:testId/fixtures` upload, `GET …/fixtures` list) · `backend/src/middleware/permissions.json` (qa_lead+ on mutations) · `frontend/src/pages/TestDetail.jsx` (fixture upload + preview) · `frontend/src/components/run/StepResultsView.jsx` (per-iteration sub-table) · `backend/tests/fixture-iteration.test.js`

**Acceptance criteria:**
- Uploading a 5-row CSV fixture to a test produces 5 iteration results on the next run, each with an `iterationIndex` field.
- Per-project iteration cap (default 10, max 100) so a 10k-row CSV can't exhaust the worker pool.
- Tests without fixtures behave identically to today (single iteration, no sub-table) — zero regression.
- Failed iterations are attributable to a specific row (`iterationIndex` + row snapshot in the result payload).
### 3 · DIF-012 — Multi-environment support (staging vs. production)
**Effort:** L | **Priority:** 🟢 Differentiator | **Dependencies:** ACL-001 ✅ (workspace scoping), INT-002 ✅ (PR #15 — check-name can carry the environment suffix once env scoping lands) | **Source:** `ROADMAP.md` Phase 3 (DIF-012) — flagged in the ROADMAP recommended-order block as "lowest effort / highest immediate enterprise value"
There is no concept of environments per project. Teams need to run the same test suite against `staging.myapp.com` and `myapp.com` separately, with per-environment run history and independent pass/fail status. This is a critical enterprise-procurement requirement and pairs well with INT-002 ✅ (the GitHub Check Run already lands per-run; with multi-env it can reflect which environment a run targeted via a check-name suffix like `Sentri QA — staging`) and DIF-010 (multi-auth profiles — staging usually has a different login surface). Add an `environments` table per project (`name`, `baseUrl`, `credentials`); scope each run to an environment; surface per-environment pass rates on the dashboard.
**Files:** new migration — `environments` table keyed on `(projectId, name)` with `baseUrl`, `credentials` (AES-encrypted JSON), `createdAt` · new `backend/src/database/repositories/environmentRepo.js` · `backend/src/routes/projects.js` (environment CRUD) · `backend/src/routes/runs.js` + `backend/src/routes/trigger.js` (accept `environmentId` and override `project.url` for the run scope only) · `backend/src/middleware/permissions.json` (admin on mutations, qa_lead on read) · `frontend/src/pages/ProjectDetail.jsx` (environment management panel) · `frontend/src/components/run/RunRegressionModal.jsx` (environment selector) · `frontend/src/pages/Dashboard.jsx` (per-environment pass-rate column) · `backend/tests/environments.test.js`

**Acceptance criteria:**
- A project with `staging` + `production` environments can run the same approved-test set against each, with independent run history per environment.
- Existing projects without any environment row continue to behave as today (run against `project.url`) — zero regression, no migration backfill required.
- Environment switching never mutates `project.url`; it overrides the per-run target only.
- Dashboard shows per-environment pass rate and a separate "Last green run" timestamp per environment.

> **Phase 5 audit-hardening blockers** (`SEC-004` MFA, `SEC-006` PII firewall, `INF-007` OTel/Sentry, `INF-008` Postgres-default + dual-DB CI matrix, `AUTO-022` AI eval harness) remain queued in `ROADMAP.md` Phase 5 — interleave with feature delivery per the "Recommended PR order" block at the bottom of ROADMAP.md.

---

## ✅ Recently completed

| ID | Title | PR |
|----|-------|----|
| INT-002 | GitHub PR check comments. New `backend/src/integrations/githubChecks.js` GitHub App Check Run client minting RS256 JWTs via `crypto.sign("RSA-SHA256")` (no external JWT library) and caching installation tokens with TTL refresh (60s skew before expiry) so concurrent check-run calls reuse the same token. Bounded retry on 429 / 5xx (3 attempts, exponential backoff capped at 2s, honours `Retry-After` header). Native `queued → in_progress → success / failure / neutral` lifecycle wired into `backend/src/routes/trigger.js`: `prepareGithubCheck()` creates the pending check on enqueue and `concludeGithubCheck()` fires from the `onComplete` hook AFTER `runRepo.save()` (never inside a DB transaction). HMAC-SHA256 verified `POST /api/v1/projects/:id/trigger/github` endpoint with event filtering (`pull_request.{opened,synchronize,reopened,ready_for_review}` + `check_suite.{requested,rerequested}`) — all other events including `ping` ack 200 + `ignored: true` so GitHub stops retrying. Idempotency keyed on `X-GitHub-Delivery` UUID (not commit SHA) via cross-dialect `LIKE`-based `runRepo.findByGithubDeliveryId()` lookup with SQL-LIKE wildcard escaping — works on both SQLite and Postgres without the breakage that would have come from `json_extract` (Postgres adapter has no translation rule for it). Summary markdown rendered by new `backend/src/utils/runResultFormatters.js`: regressed-tests only (failing now AND green on base SHA's last run), explicit "no green base run found" fallback when the 25-run `BASE_LOOKBACK_RUNS` window has no qualifying green ancestor, separate `### Web Vitals budget violations` markdown section so vitals failures don't get lost in the test-failure list. Per-project Settings → Integrations tab (`frontend/src/pages/Settings.jsx` IntegrationsTab) gated on `qa_lead` read / `admin` write via `permissions.json`; new `github_check_settings` table (migration `021_run_github_check.sql`) holds per-project `enabled` + `installationId` + `repo`. New `githubCheck` JSON column on `runs` (also migration 021) registered in `runRepo.JSON_FIELDS` + `INSERT_COLS`. Lean `runRepo.getRecentTestRunsForGithubBase(projectId, 25)` accessor selects only `id/type/status/failed/githubCheck/results` for the base-run lookup so a project with hundreds of runs doesn't trigger heavy JSON deserialisation on every check completion. GitHub 5xx swallowed + logged (never fails the underlying Sentri run) per the INT-002 anti-pattern guard. `req.rawBody` capture extended to the `/trigger/github` path. New `backend/tests/github-checks.test.js` (9 tests, registered in `run-tests.js`) covers payload shape + installation-token caching, regressed-diff with `baseRun`, fallback to all-failing when no green base, `findGreenBaseRun` bounded lookup + repo+SHA match, Web Vitals violation rendering + conclusion=failure, 5xx exhaustion surfaces to caller (so the integration hook can swallow), transient 5xx recovery on retry, `findByGithubDeliveryId` idempotency (DB integration), `Retry-After` 429 honouring. `docs/api/projects.md` + `docs/changelog.md` + `backend/.env.example` + `docs/guide/env-vars.md` updated. | #15 |
| AUTO-001 | Risk-based test selection / ordering. Pure-function scorer `backend/src/pipeline/riskScorer.js` weighting per-test pass rate from `runs.results[]`, `tests.updatedAt` recency boost, self-heal frequency, and AUTO-002's `changedPages[]` (strongest signal — change-affected tests surface to the top). `normalizeBudgetMinutes()` server-side clamp at `MAX_BUDGET_MINUTES=240` so a malformed body param can't exhaust the worker pool. Smoke-test pin via tags `["smoke"]` or `smoke` substring in name — pinned regardless of budget truncation, enforced at the runner layer in `testRunner.js`. Dispatch reorder happens at the routes layer (`runs.js` + `trigger.js`) and in the BullMQ worker; **persisted** `testQueue` preserves the original approved-test order with per-row `riskScore`, so the saved run reflects what the reviewer queued (audit fidelity), not how the runner scheduled it. Budget-skipped tests are pre-seeded into `results` as `{ status: "skipped", skipReason: "over_budget" }` markers so every approved test has an observable resolution. Trigger-token path (`routes/trigger.js`) byte-aligned with JWT path (`routes/runs.js`): `buildTestRun()` shared shape, activity-log + `trackTelemetry` report dispatched (not approved) counts. `RunDetail.jsx` surfaces a `riskScore` chip per test row + "skipped (over budget)" status badge + "budget: Nm" label on the run header. New `backend/tests/risk-scorer.test.js` (registered in `run-tests.js`) covers flaky-test ranking, recently-edited boost, smoke-test pin, budget truncation with skipped-resolution surfacing, malformed/oversized budget clamp, runner-level smoke-pin invariant, BullMQ worker order-preservation invariant, and changedPages weighting. New `docs/AUDIT_IMPL.md` lands as the implementation companion to `AUDIT.md` (informational; no runtime effect). | #15 |
| AI-001 | Generic OpenAI-compatible provider slots (BYO endpoint). New `compat:<id>` provider type (`backend/src/aiProvider.js` + `backend/src/database/repositories/apiKeyRepo.js` `listCompatSlots` / `getCompatSlot` / `setCompatSlot` / `deleteCompatSlot`) persisting `{ baseUrl, model, apiKey, displayName }` as AES-encrypted JSON. Per-call SSRF-guarded fetch wrapper (`createSsrfGuardedFetch()`) re-validates the baseURL on every OpenAI SDK call (DNS-rebinding mitigation) and sets `redirect: "error"` to block 3xx escapes to private / link-local / cloud-metadata addresses. New TTL cache `backend/src/utils/compatConfigCache.js` (default 60s, `COMPAT_CONFIG_CACHE_TTL_MS` override) with write-through invalidation on `setCompatSlot` / `deleteCompatSlot` and Redis pub/sub coherence across instances (`sentri:compat-config:invalidate`, self-echo suppressed via origin id). Compat slots participate in auto-detection, FEA-003 fallback chain, per-slot circuit breakers, and both `callProvider` / `streamText` paths. Backend-enforced slot-id validation (`^[a-z0-9_-]+$`), required-field checks, and async `validateUrl()` SSRF on compat saves. `ALLOW_PRIVATE_URLS=true` escape hatch **scoped exclusively** to compat saves + the guarded fetch — trigger callbacks, preview URLs, and notification webhooks remain SSRF-protected. Settings UI with `<datalist>` hints for common vendors (DeepSeek / Groq / Mistral / xAI) + Edit/Delete per slot; `ProviderBadge` renders deterministic palette colors (`COMPAT_PALETTE` hashed by slot id). New tests `backend/tests/openai-compat-provider.test.js` + `backend/tests/compat-config-cache.test.js` (registered in `run-tests.js`) cover error classification, SSRF rejection, `ALLOW_PRIVATE_URLS` scoping, mock-SDK baseURL routing + per-slot circuit-breaker independence, per-call DNS-rebinding mitigation, read-through cache, TTL expiry, write-through invalidation, cross-process coherence, self-echo suppression. Gitleaks allowlist updated. | #14 |
| AUTO-002 + AUTO-015 (bundled) | Change detection / diff-aware crawling (`crawl_baselines` table + `crawlBaselineRepo` with `mergeProjectBaselines` partial-crawl-safe upserts + `crawlDiff` primitive reusing `stateFingerprint.js`) + continuous test discovery on deployment events (Vercel `X-Vercel-Signature` HMAC-SHA1 + Netlify `X-Netlify-Token` HMAC-SHA256 webhooks, dual-auth via `requireTrigger` + signature, SSRF-guarded preview URL, `triggerCrawl: true` on POST /trigger). Shared `runDiffAwareBaseline(project, run, snapshots, mode)` helper handles **both** link-crawl and state-explorer modes — state mode uses composite keys (`url#fp=<fingerprint>`) so distinct states at the same URL (AUTO-002b) track as separate baseline rows. `pages_changed` SSE event wired into Test Lab live view via `useProjectRunMonitor` → `ActiveRunBanner` ("N pages changed → regenerating only those" replaces generic progress bar). Migration `019_crawl_baselines.sql` + migration `020_run_changed_pages.sql` (`runs.changedPages` + `runs.removedPages` JSON columns registered in `runRepo.JSON_FIELDS` + `INSERT_COLS`). `canonicalUrl` preservation on preview crawls prevents production-baseline corruption (`project.url` is overridden to `previewUrl` in `trigger.js` but `project.canonicalUrl` is set to the original, used by `sameOrigin` guard). `crawl.start.deployment` activity marker (AUTO-015b) + new `GET /api/v1/projects/:id/last-deployment-run` route powers the "Last deployment run" chip on `ProjectHeader.jsx` (24h window, navigates to run on click). `dialsConfig` honoured on `triggerCrawl` path + webhook-launched preview crawls. `req.rawBody` capture scoped to webhook routes only (avoids global Buffer copy). End-to-end happy-path test in `deployment-triggers.test.js` asserts webhook → run dispatch + activity marker; `crawl-diff.test.js` covers all 8 scenarios (added/changed/unchanged/removed, first-crawl fallback, null/undefined baseline, no-change, empty current crawl, state-mode composite keys, fingerprint stability); dedicated `crawl-baseline-repo.test.js` per REVIEW.md. AGENT.md gained new "Issue-handling rule" section codifying the "every finding produces an outcome (fix or ROADMAP entry), never a silent gap" norm. | #12 |

*Full completed list → ROADMAP.md § Completed Work*
